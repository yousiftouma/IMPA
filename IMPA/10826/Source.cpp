#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main ()
{
	int N;
	while(cin >> N)
	{
		if (N == 0) return 0;
		vector<int> numbers;
		for (int i = 1; i <= N; i++)
		{
			numbers.push_back(i);
		}

	}

	return 0;
}